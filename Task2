const arr: number[] = [1, 2, 3, 4, 5, 6, 7, 8, 9];

function arrSum(nums: number[]): number {
    let sum = 0;
    nums.forEach(num => {
        sum += num;
    });
    return sum;
}

console.log(arrSum(arr)); 
